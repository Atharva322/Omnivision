# Local Core ML model

`MobileFaceNet.mlpackage` is generated locally by `Tools/models/convert_buffalo_sc.py` and is
intentionally ignored by Git. It contains restricted, non-commercial InsightFace weights.

See `docs/MODEL_LICENSE.md` and `docs/FACE_EMBEDDING_PLAN.md` before generating or using it.
