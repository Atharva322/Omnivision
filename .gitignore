.DS_Store
.env
.env.local

# Xcode
build/
DerivedData/
xcuserdata/
*.xcuserstate

# SwiftPM (Track C package)
.build/
.swiftpm/
Package.resolved

# Private notes — never committed
notes/
# ShopAssist/backend (Python)
.venv/
__pycache__/
*.pyc
.pytest_cache/

# macOS
.DS_Store

# Demo videos — too large for GitHub (>100MB), hosted externally instead. See demo/README.md.
demo/*.MOV
demo/*.mov
demo/*.mp4
